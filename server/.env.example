# Port the HTTP + Socket.IO server listens on
PORT=5000

# Comma-separated list of allowed client origins in production (only used
# when NODE_ENV=production). E.g. your Cloudflare Pages URL and any custom
# domain: https://pianojam.pages.dev,https://pianojam.example.com
CLIENT_ORIGIN=
