# Base URL of the Socket.IO/API server in production (e.g. your Render URL,
# https://pianojam-server.onrender.com). Leave unset for local dev, where
# Vite proxies /api and /socket.io to localhost:5000, and for any deploy
# where the client and server share an origin.
VITE_SERVER_URL=
